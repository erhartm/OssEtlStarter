# Demo Environment Files

- `etl.env`: Environment variables for ETL jobs (SQL Server, MongoDB, Delta paths).
- `client.env`: Environment variables for the Python client (Spark master, gold view name).

Edit these files as needed for your demo or local setup.
